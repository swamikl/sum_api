
// JS file that can handel all the interactions with the PG database 

// const { Pool } = require('pg');
// const pool = new Pool();

const setNum = (num) => {
    console.log("got to save num")
}

const getSum = (num) => {
    console.log("got to get sum")
}